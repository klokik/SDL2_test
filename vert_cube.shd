#version 120

uniform mat4 u_modelview;

uniform mat4 u_projection;

attribute vec3 a_pos;
attribute vec3 a_normal;

varying vec4 pos;

varying vec3 nrm;

void main(void)
{
	pos  = u_modelview  * vec4(a_pos,1.0);
	nrm = (u_modelview * vec4(a_normal,1.0)).xyz;

	gl_Position = u_projection * pos;
}